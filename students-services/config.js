const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "",
        database : "transactions",
        connectTimeout: 60,
    },
    listPerPage : 10,
};

module.exports = config;